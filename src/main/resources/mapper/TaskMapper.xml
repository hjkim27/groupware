<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hjkim27.dev.mapper.TaskMapper">

    <!--사용자 정보 조회 -->
    <resultMap id="returnUser" type="hjkim27.dev.bean.user.UserDTO">
        <result property="sid" column="sid"/>
        <result property="loginId" column="login_id"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="birth" column="birth"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <!-- 계정 정보 찾기 -->
    <resultMap id="findInfo" type="hjkim27.dev.bean.user.UserDTO">
        <result property="sid" column="sid"/>
        <result property="loginId" column="login_id"/>
        <result property="email" column="email"/>
    </resultMap>

    <!-- ====================================================================== -->

    <!-- 사용자 정보 업데이트 -->
    <update id="update" parameterType="hjkim27.dev.bean.task.TaskDTO">
        update tb_task
        <trim prefix="set" suffixOverrides=",">
            <if test="title != null">title = #{title},</if>
            <if test="description != null">description = #{description},</if>
            <if test="priority != null">priority = #{priority},</if>
            <if test="dueDate != null">due_date = #{dueDate},</if>
            <if test="assignedUserSid != null">
                assingned_user_sid = #{assignedUserSid,typeHandler=hjkim27.dev.handler.IntegerListTypeHandler},
            </if>
            <if test="tagSid != null">
                tag_sid = #{tagSid,typeHandler=hjkim27.dev.handler.IntegerListTypeHandler},
            </if>
            <if test="groupSid != null">group_sid = #{groupSid},</if>
            <if test="startDate != null">start_date = #{startDate},</if>
        </trim>
        where sid = #{sid}
    </update>

    <!-- 사용자 조회 -->
    <!--todo 그룹sid 로 그룹명 조회하도록 수정 필요 + 그룹테이블 join -->
    <select id="get" resultMap="returnUser" parameterType="hjkim27.dev.bean.user.UserSearch">
        select
        sid, title, description, status, priority,
        due_date, created_user_sid, assigned_user_sid, view_user_sids
        from tb_task
        <include refid="search"/>
        limit 1
    </select>

    <!-- 사용자 목록 조회 -->
    <!--todo 그룹sid 로 그룹명 조회하도록 수정 필요 + 그룹테이블 join -->
    <select id="getAll" resultMap="returnUser" parameterType="hjkim27.dev.bean.user.UserSearch">
        select
        sid, title, status, priority, due_date, created_user_sid, assigned_user_sid
        from tb_task
        <include refid="search"/>
        <include refid="orderBy"/>
        <if test="usePaging">
            limit #{limit} offset #{offset}
        </if>
    </select>

    <!-- 사용자 전체 수 -->
    <select id="getCount" resultType="int" parameterType="hjkim27.dev.bean.user.UserSearch">
        select count(*) from tb_task
        <include refid="search"/>
    </select>

    <!-- ====================================================================== -->

    <sql id="search">
        <trim prefix="where" prefixOverrides="and">
            <if test="searchValue != null and searchValue != ''">
                <choose>
                    <when test="searchKey == 'title'">and title ilike '%'||#{searchValue}||'%'</when>
                </choose>
            </if>
            <if test="sid != 0">and sid = #{sid}</if>
            <!-- 날짜 검색 -->
            <if test="dateColumnIdx != -1">
                <choose>
                    <when test="dateColumnIdx == 1">created_at &gt;= #{startDate} and created_at &lt;= #{endDate}</when>
                    <when test="dateColumnIdx == 2">updated_at &gt;= #{startDate} and updated_at &lt;= #{endDate}</when>
                </choose>
            </if>
        </trim>
    </sql>

    <sql id="orderBy">
        <trim suffix="order by">
            <choose>
                <when test="columnIdx == 2">title</when>
                <when test="columnIdx == 3">status</when>
                <when test="columnIdx == 4">priority</when>
                <when test="columnIdx == 5">due_date</when>
                <when test="columnIdx == 6">created_user_sid</when>
                <when test="columnIdx == 7">assigned_user_sid</when>
                <otherwise>sid</otherwise>
            </choose>
            <if test="desc">desc</if>
        </trim>
    </sql>
</mapper>